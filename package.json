{
  "name": "coverage-action",
  "version": "1.0.0",
  "description": "An action that computes and uploads golang coverage.txt output to prometheus",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --platform=node --outfile=dist/index.js --external:@actions/core --external:@actions/github",
    "test": "jest"
  },
  "keywords": [],
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0",
    "axios": "^1.13.6"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "jest": "^29.7.0"
  }
}
